package pl.lukasz.culer.utils

class Consts {
    companion object {
        const val T_RULE_MEMBERSHIP = 1.0         //default and constant terminal rule membership
        const val N_RULE_MEMBERSHIP = 0.0         //just initial non-terminal rule membership

        const val DEFAULT_START_SYMBOL = '$'

        const val N_GEN_START = 'A'
    }
}